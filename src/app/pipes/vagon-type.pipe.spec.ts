import { VagonTypePipe } from './vagon-type.pipe';

describe('VagonTypePipe', () => {
  it('create an instance', () => {
    const pipe = new VagonTypePipe();
    expect(pipe).toBeTruthy();
  });
});
